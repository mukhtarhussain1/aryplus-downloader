import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "ARY Plus Downloader — Free, High-Speed HD Video Downloader",
  description: "Download your favorite ARY Plus dramas, shows, and episodes in crisp 1080p MP4. Free, lightning fast, with zero quality loss and no registration needed.",
  keywords: "ary plus downloader, download ary plus dramas, pakistani drama downloader, ary plus video download 1080p, free ary mp4",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
